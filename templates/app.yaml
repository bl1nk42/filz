name: app
description: สร้างแอปพลิเคชัน
type: work
stack: rust

folders:
  - src
  - tests
  - docs

files:
  - path: Cargo.toml
    content: |
      [package]
      name = "NAME"
      version = "0.1.0"
      edition = "2021"

      [dependencies]

  - path: src/main.rs
    content: |
        fn main() {
          println!("Hello from NAME!");
        }

  - path: README.md
    content: |
      # NAME
      แอปพลิเคชันที่สร้างด้วย Rust

  - path: meta.json
    content: |
      {
        "name": "NAME",
        "type": "TYPE",
        "stack": "STACK",
        "created": "CREATED"
      }
